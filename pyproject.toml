[project]
name = "cs_logistics"
authors = [
    { name = "CS Logistics", email = "info@cslogistics.com" }
]
description = "CS Logistics dashboard custom application."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "cs_logistics"
